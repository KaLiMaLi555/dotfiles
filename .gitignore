# Backups & logs
.backup/
log/

# Rendered-at-install (kept out of repo; regenerated by render.sh)
stow/tmux/.config/tmux/workspaces/dev.conf

# Local overlay accidentals (these should live in $HOME, not the repo)
*.local
.zshrc.local
.gitconfig.local

# OS / editor noise
.DS_Store
*.swp
*.bak
*~

# Inside vendored configs
stow/nvim/.config/nvim/lazy-lock.json.bak

# Claude Code session noise that may end up under tmux configs we copied
stow/**/.claude/
